apiVersion: 1

providers:
  - name: DMF Benchmarks
    orgId: 1
    folder: DMF Benchmarks
    type: file
    disableDeletion: true
    allowUiUpdates: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/dashboards
